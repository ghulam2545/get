```
git init
git status
git add filename.ext
OR 
git add .  // to add all

git push -u origin master
git push

git branch              // see all branch
git branch branch-name  // create a new branch
git switch branch-name  // switch to a branch
git branch --delete branch-name // to delete a branch


git clone your-git-repository.git

- TODO
- git fetch
- git pull
- git merge
```